#pragma once

namespace zrn {

class Texture {
private:
    /* data */
public:
    Texture(/* args */);
    ~Texture();
};

Texture::Texture(/* args */) {
}

Texture::~Texture() {
}

} // namespace zrn
